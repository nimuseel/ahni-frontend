# Development Commands

## Prerequisites

- Flutter stable channel
- Android Studio or Xcode for device builds

## Commands

```bash
flutter pub get
flutter analyze
flutter test
flutter run
./scripts/verify
```

Use the Flutter SDK selected by the project environment. Keep `pubspec.lock` committed for this application.
