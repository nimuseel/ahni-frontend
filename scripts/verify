#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

bash test/scripts/dev_test.sh
./scripts/lint
./scripts/typecheck
./scripts/test-unit
./scripts/test-widget
./scripts/test-integration
